"""
Reserved for computation that goes beyond a straight SQL query — e.g.
correlation analysis, custom scoring, or anything combining multiple
query-layer results in pandas before display.

Currently empty. Add modules here as needed; pages import from both
queries/ (raw data) and analysis/ (derived data) as required.
"""
